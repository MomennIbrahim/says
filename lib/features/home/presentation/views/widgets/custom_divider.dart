import 'package:flutter/material.dart';

class CustomDiviver extends StatelessWidget {
  const CustomDiviver({super.key});

  @override
  Widget build(BuildContext context) {
    return const  Divider(color: Color.fromRGBO(0, 0, 0, 0.25),);
  }
}
